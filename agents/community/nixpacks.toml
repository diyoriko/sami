[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gnumake", "gcc", "yt-dlp", "ffmpeg"]

[phases.install]
cmds = ["npm install", "npm install -g @anthropic-ai/claude-code"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
