[phases.setup]
nixPkgs = ["nodejs_22", "python3", "python3Packages.pip", "yt-dlp", "gnumake", "gcc", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
